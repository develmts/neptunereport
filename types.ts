export interface Partner {
  id: string;
  name: string;
  role: string;
  specialties: string[];
  bio: string;
  image: string;
  detailedBio: string;
  experience: string[];
  email: string;
}

export interface CarouselItem {
  title: string;
  subtitle: string;
}
